# Quality utilities

The repository-root UTF-8 check is kept in `scripts/check_utf8.py`. Run it after changes to Russian user-facing text, fixtures, or migrations. Frontend and backend test commands are documented in the main README.
